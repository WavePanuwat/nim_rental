'use client';

import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  typography: {
    fontFamily: 'var(--font-anuphan)', // เปลี่ยนชื่อตัวแปรตรงนี้
  },
});

export default theme;